{
  "name": "@meshnet/mobile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "The phone shell, iOS and Android: Capacitor around the built client, with the BLE plugin the client calls. No `build`, `typecheck` or `test` script: `pnpm -r` runs on machines with no Xcode and no Android SDK. An iOS build needs a Mac; see README.md here.",
  "scripts": {
    "sync": "pnpm --filter @meshnet/meshcore build && pnpm --filter @meshnet/web build && cap sync",
    "android": "pnpm sync && cap open android",
    "ios": "pnpm sync && cap open ios",
    "android:apk": "node scripts/android.mjs apk",
    "android:aab": "node scripts/android.mjs aab"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^8.3.0",
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@capacitor/ios": "8.5.2",
    "@capacitor/keyboard": "^8.0.5",
    "@capacitor/local-notifications": "^8.3.1",
    "capacitor-secure-storage-plugin": "0.13.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
