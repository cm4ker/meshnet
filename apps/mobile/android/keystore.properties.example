# Copy to keystore.properties (ignored by Git), then fill in your upload key.
# Use an absolute path with forward slashes on Windows.
storeFile=C:/Users/your-user/.android/ommesh-upload.jks
storePassword=replace-with-store-password
keyAlias=ommesh-upload
keyPassword=replace-with-key-password
