{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notices",
  "description": "The notices window: it hears the cards the shell hands it and nothing more; what is done on a card goes back through the shell's own commands.",
  "windows": ["notices"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
