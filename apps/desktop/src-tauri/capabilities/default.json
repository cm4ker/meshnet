{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The client's window: the two links to a radio, and the system browser for links in messages. Nothing else is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "blec:default",
    "serialplugin:default",
    "opener:default"
  ]
}
